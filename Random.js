#!/usr/bin/env node

/**
 * There are two was to generate random no. in Js. 
 *  a. Math.random 
 *  b. crypto.getRandomValue()
 */

let random = Math.random(); // Returns the random no. including 0 and excluding 1. 

console.log(random);
